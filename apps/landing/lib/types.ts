/**
 * Moved to @landing-kit/types — the public landing payload contract is now
 * shared with the admin so both render from one definition. This thin
 * re-export keeps the existing local import path ("@/lib/types") working.
 * Edit packages/landing-kit/types.ts.
 */
export * from "@landing-kit/types";
