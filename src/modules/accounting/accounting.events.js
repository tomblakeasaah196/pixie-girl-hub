/**
 * Accounting & Finance (V2.2 §6.6) — domain events.
 */

"use strict";

const { createModuleEvents } = require("../../shared/events/module-events");

module.exports = createModuleEvents("accounting");
