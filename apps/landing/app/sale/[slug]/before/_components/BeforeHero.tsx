/**
 * Moved to @landing-kit (shared with the admin's Atelier preview) so the live
 * "before" hero and the admin preview render from one component.
 * Edit packages/landing-kit/BeforeHero.tsx.
 */
export { BeforeHero } from "@landing-kit/BeforeHero";
