/**
 * Moved to @landing-kit (shared with the admin Landing Studio). This thin
 * re-export keeps the existing local import path working.
 * Edit packages/landing-kit/AtelierRevealPreview.tsx.
 */
export { AtelierRevealPreview } from "@landing-kit";
