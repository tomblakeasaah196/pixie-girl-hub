/**
 * Moved to @landing-kit — the shared renderer used by BOTH the public sales
 * site and the admin Landing Studio, so the live page can't drift from the
 * studio preview. Compose it with <AtelierRevealPreview> via PublicLanding.
 *
 * Edit packages/landing-kit/LandingPreview.tsx.
 */
export { LandingPreview, type LandingSubmit } from "@landing-kit";
