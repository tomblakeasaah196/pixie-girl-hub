# Hub backend the SSR server talks to (browser uses same-origin /api proxy)
HUB_API_URL=http://localhost:7000
# Dev proxy target for vite (defaults to http://localhost:7000)
VITE_API_PROXY_TARGET=http://localhost:7000
